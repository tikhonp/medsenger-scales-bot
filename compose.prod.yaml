services:
  server:
    container_name: agents-scales-server
    image: docker.telepat.online/agents-scales-image:latest
    restart: unless-stopped
    entrypoint: |
      /bin/sh -c '
        goose postgres "$(get_db_string)" -dir=migrations up
        server
      '
    network_mode: "host"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - .env
    environment:
      - SOURCE_COMMIT=${SOURCE_COMMIT}
      - DEBUG=false
